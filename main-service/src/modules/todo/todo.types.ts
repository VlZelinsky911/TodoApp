export enum Priority {
  Low = "low",
  Medium = "medium",
  High = "high",
}

export enum Status {
  All = "all",
  Active = "active",
  Completed = "completed",
}

export enum SortBy {
  CreatedAt = "createdAt",
  DueDate = "dueDate",
  Priority = "priority",
}

export enum SortOrder {
  Asc = "asc",
  Desc = "desc",
}
